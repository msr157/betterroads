-- Metadata-only baseline: migrations 0005 and 0006 were authored additively
-- to preserve anonymous production data. The matching 0007 snapshot records
-- their resulting schema so future drizzle-kit generations remain incremental.
SELECT 1;
