VITE_API_URL=http://localhost:3000
# Optional: leave VITE_API_URL unset and proxy local /api requests to this host.
# Use https://betterroads.org to inspect the live public-map dataset locally.
VITE_DEV_API_PROXY=http://localhost:3000
# Private homepage comparison routes. Never enable in the production image.
VITE_ENABLE_CONCEPTS=false
