{
  "name": "betterroads",
  "version": "1.2.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "test": "node scripts/test-all.mjs",
    "typecheck:all": "pnpm -r run build && npm --prefix dashboard run build && npm --prefix mobile/app run typecheck",
    "build:apk": "node scripts/build-apk.mjs",
    "build:apk:test": "node -e \"process.env.BETTERROADS_RELEASE_CHANNEL='test'; import('./scripts/build-apk.mjs')\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@9.15.4",
  "dependencies": {
    "fflate": "0.8.2"
  }
}
